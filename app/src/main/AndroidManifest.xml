<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bankingapp">

    <!-- ══ PERMISSIONS ══════════════════════════════════════════════════════ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankingTransactionApp">

        <!-- ══ ACTIVITIES ════════════════════════════════════════════════ -->

        <!-- 1. MainActivity (Splash) — LAUNCHER -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 2. LoginActivity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"/>

        <!-- 3. RegisterActivity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"/>

        <!-- 4. DashboardActivity — needs ActionBar for 3-dots menu -->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.BankingTransactionApp.DarkActionBar"/>

        <!-- 5. TransactionActivity — needs ActionBar for back button + title -->
        <activity
            android:name=".activities.TransactionActivity"
            android:exported="false"
            android:theme="@style/Theme.BankingTransactionApp.DarkActionBar"
            android:parentActivityName=".activities.DashboardActivity"/>

        <!-- 6. InsightsActivity — needs ActionBar for back button + title -->
        <activity
            android:name=".activities.InsightsActivity"
            android:exported="false"
            android:theme="@style/Theme.BankingTransactionApp.DarkActionBar"
            android:parentActivityName=".activities.DashboardActivity"/>

        <!-- 7. SettingsActivity — needs ActionBar for back button -->
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.BankingTransactionApp.DarkActionBar"
            android:parentActivityName=".activities.DashboardActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashboardActivity"/>
        </activity>

        <!-- ══ SERVICE ═══════════════════════════════════════════════════ -->
        <service
            android:name=".services.TransactionService"
            android:exported="false"/>

        <!-- ══ BROADCAST RECEIVER ════════════════════════════════════════ -->
        <receiver
            android:name=".receivers.NetworkChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

        <!-- ══ CONTENT PROVIDER ══════════════════════════════════════════ -->
        <provider
            android:name=".providers.TransactionProvider"
            android:authorities="com.bankingapp.provider"
            android:exported="false"/>

    </application>

</manifest>